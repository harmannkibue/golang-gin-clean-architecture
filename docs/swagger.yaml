basePath: /api/v1
definitions:
  httputil.HTTPError:
    properties:
      code:
        example: 400
        type: integer
      message:
        example: status bad request
        type: string
    type: object
  intfaces.MpesaRatibaRequestBody:
    properties:
      AccountReference:
        type: string
      Amount:
        type: string
      BusinessShortCode:
        type: string
      CallBackURL:
        type: string
      EndDate:
        type: string
      Frequency:
        type: string
      PartyA:
        type: string
      ReceiverPartyIdentifierType:
        type: string
      StandingOrderName:
        type: string
      StartDate:
        type: string
      TransactionDesc:
        type: string
      TransactionType:
        type: string
    type: object
  intfaces.MpesaRatibaRequestResponseBody:
    properties:
      ResponseBody:
        properties:
          responseCode:
            type: string
          responseDescription:
            type: string
        type: object
      ResponseHeader:
        properties:
          responseCode:
            type: string
          responseDescription:
            type: string
          responseRefID:
            type: string
        type: object
    type: object
host: localhost:8080
info:
  contact: {}
  description: |-
    Spectacular Billing Golang Payment Services Connector Clean Architecture using Uncle Bob's clean architecture principles.
    It serves as Blog.
  title: Spectacular SAS Billing.
  version: "1.0"
paths:
  /mratiba/standing-orders:
    post:
      consumes:
      - application/json
      description: Create M-Pesa Ratiba standing order
      operationId: Create M-Pesa Ratiba standing order
      parameters:
      - description: Create Mpesa Ratiba standing order for subscription management
        in: body
        name: request
        required: true
        schema:
          $ref: '#/definitions/intfaces.MpesaRatibaRequestBody'
      produces:
      - application/json
      responses:
        "201":
          description: Created
          schema:
            $ref: '#/definitions/intfaces.MpesaRatibaRequestResponseBody'
        "400":
          description: Bad Request
          schema:
            $ref: '#/definitions/httputil.HTTPError'
        "500":
          description: Internal Server Error
          schema:
            $ref: '#/definitions/httputil.HTTPError'
      summary: Create M-Pesa Ratiba standing order
      tags:
      - Mpesa Ratiba
securityDefinitions:
  BasicAuth:
    type: basic
swagger: "2.0"
